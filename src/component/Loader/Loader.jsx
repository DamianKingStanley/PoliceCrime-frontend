// Loader.js
import React from "react";
import "./Loader.css"; // Make sure to create and use Loader.css for styling

const Loader = () => (
  <div className="loader-overlay">
    <div className="loader"></div>
  </div>
);

export default Loader;
